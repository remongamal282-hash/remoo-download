# ✅ v0.5 جاهزة للتحقق اليدوي

## 📦 الحالة الحالية

**جميع المهام مكتملة (8/8)** ✓

### البناء الحالي
- ✅ Build موجود: `build/gui-debug/`
- ✅ Executables موجودة:
  - `build/gui-debug/bin/remo_service.exe` (545 KB)
  - `build/gui-debug/bin/remo_cli_client.exe` (545 KB)
  - `build/gui-debug/remo_native_host.exe` (283 KB)
- ✅ Extension موجودة: `extensions/chromium/`
- ✅ Scripts موجودة: `tools/register_native_host.ps1`

## 🚀 ابدأ التحقق الآن

### الخطوة 1: تحديث المسارات

**المسارات الصحيحة بعد الإصلاح:**

```powershell
# مسار remo_service
d:\remoo-download\build\gui-debug\bin\remo_service.exe

# مسار remo_cli_client
d:\remoo-download\build\gui-debug\bin\remo_cli_client.exe

# مسار remo_native_host (✅ الآن في bin مع الـ DLLs)
d:\remoo-download\build\gui-debug\bin\remo_native_host.exe
```

**✅ تم إصلاح مشكلة "Native host has exited":**
- نقل `remo_native_host.exe` إلى `bin/` مع جميع الـ DLLs
- تحديث CMakeLists.txt للبناء المستقبلي
- إعادة تسجيل في Registry بالمسار الصحيح

### الخطوة 2: تشغيل الخدمة

افتح PowerShell **Terminal 1**:

```powershell
cd d:\remoo-download\build\gui-debug\bin
.\remo_service.exe
```

**لا تغلق هذا Terminal** - اتركه يعمل في الخلفية.

### الخطوة 3: اختبار CLI (Baseline)

افتح PowerShell **Terminal 2**:

```powershell
cd d:\remoo-download\build\gui-debug\bin
.\remo_cli_client.exe add-download "https://proof.ovh.net/files/10Mb.dat" -d "D:\TestDownloads"
```

**النتيجة المتوقعة:**
```
[remo_cli_client] Connecting to IPC pipe \\.\pipe\remo_download_ipc...
[remo_cli_client Request]  {"command":"addDownload","url":"https://proof.ovh.net/files/10Mb.dat",...}
[remo_cli_client Response] {"ok":true,"status":"accepted","downloadId":1}
```

### الخطوة 4: التحقق من الملف

```powershell
# راقب الملف كل ثانيتين
while ($true) {
    Get-ChildItem "D:\TestDownloads\*.dat" | Select-Object Name, @{Name="Size(MB)";Expression={[math]::Round($_.Length/1MB,2)}}
    Start-Sleep -Seconds 2
}
```

**توقف عندما يصل الملف إلى 10.00 MB** (اضغط Ctrl+C)

✅ **إذا نجح هذا، فالسلسلة الأساسية تعمل: IPC → Service → DownloadEngine → Disk**

---

### الخطوة 5: تسجيل Native Host

```powershell
cd d:\remoo-download

# التسجيل مع المسار الصحيح (في bin مع الـ DLLs)
powershell -ExecutionPolicy Bypass -File tools\register_native_host.ps1 -HostExecutablePath "d:\remoo-download\build\gui-debug\bin\remo_native_host.exe"
```

**النتيجة المتوقعة:**
```
======================================
Remoo Download Native Host Registration
======================================

Mode: REGISTER
Host Executable: d:\remoo-download\build\gui-debug\remo_native_host.exe

Registering for Chrome...
  ✓ Successfully registered for Chrome

Registering for Edge...
  ✓ Successfully registered for Edge

Done!
```

### الخطوة 6: تحميل الإضافة في Chrome

1. افتح Chrome: `chrome://extensions`
2. فعّل **Developer mode** (أعلى اليمين)
3. اضغط **Load unpacked**
4. اختر: `d:\remoo-download\extensions\chromium`
5. **انسخ Extension ID** (مثال: `abcdefghijklmnopqrstuvwxyzabcdef`)

### الخطوة 7: إعادة التسجيل بـ ID الحقيقي

```powershell
# استبدل YOUR_EXTENSION_ID بالـ ID المنسوخ
powershell -ExecutionPolicy Bypass -File tools\register_native_host.ps1 -HostExecutablePath "d:\remoo-download\build\gui-debug\bin\remo_native_host.exe" -ExtensionId "YOUR_EXTENSION_ID"
```

### الخطوة 8: أعد تشغيل Chrome

**أغلق Chrome تمامًا** ثم افتحه مرة أخرى.

### الخطوة 9: اختبار Popup

1. اضغط على أيقونة Extensions (قطعة puzzle)
2. افتح **Remoo Download Integration**
3. **توقع:**
   - Status badge أخضر: **"Connected"**
   - زر "Test Connection" موجود
4. اضغط **Test Connection**
5. **توقع:** رسالة نجاح خضراء

✅ **إذا نجح هذا، فالاتصال Extension → Native Host → Service يعمل!**

---

### الخطوة 10: الاختبار الحاسم - End-to-End Download

**هذا هو الاختبار الأهم!**

1. تأكد أن `remo_service.exe` لا يزال يعمل في Terminal 1
2. افتح صفحة جديدة في Chrome: `https://proof.ovh.net/files/`
3. اضغط على **10Mb.dat**

**النتيجة المتوقعة:**
- ❌ تحميل Chrome يبدأ ثم **يختفي فورًا** (ملغي)
- ✅ إشعار Chrome يظهر: **"Download Started - Remoo Download is handling this file"**
- ✅ Terminal 1 يُظهر: `[remo_service] Received request: addDownload...`

4. راقب الملف:

```powershell
# في Terminal 2 أو terminal جديد
while ($true) {
    Get-ChildItem "D:\TestDownloads\*.dat" | Select-Object Name, @{Name="Size(MB)";Expression={[math]::Round($_.Length/1MB,2)}}, LastWriteTime
    Start-Sleep -Seconds 2
}
```

**توقع:**
```
Name        Size(MB)  LastWriteTime
----        --------  -------------
10Mb.dat    2.34      15/01/2025 10:23:45
10Mb.dat    5.67      15/01/2025 10:23:47
10Mb.dat    10.00     15/01/2025 10:23:50  ← Final!
```

### الخطوة 11: التحقق النهائي

```powershell
# تحقق من الحجم الدقيق
(Get-Item "D:\TestDownloads\10Mb.dat").Length
# Expected: 10485760 (exactly 10 MB)
```

---

## 🎉 معايير النجاح

إذا تحققت **جميع** النقاط التالية، فـ v0.5 **ناجحة بالكامل**:

- ✅ `remo_service` يعمل بدون أخطاء
- ✅ `remo_cli_client` يحمّل ملفات عبر IPC
- ✅ الملفات تظهر على القرص بالحجم الصحيح
- ✅ Native host مسجل في Registry
- ✅ الإضافة تُحمّل في Chrome بدون أخطاء
- ✅ Popup يعرض "Connected"
- ✅ Test Connection ينجح
- ✅ **تحميل المتصفح يُعترض ويُلغى**
- ✅ **إشعار Chrome يظهر**
- ✅ **الملف يُحمّل عبر Remoo Download**
- ✅ **حجم الملف النهائي صحيح تمامًا**

---

## 🐛 إذا فشل شيء

### المشكلة: الخدمة لا تبدأ
- تأكد من عدم وجود عملية أخرى تستخدم نفس pipe name
- جرّب إعادة تشغيل الكمبيوتر
- تحقق من logs في terminal الخدمة

### المشكلة: CLI client لا يتصل
- تأكد أن `remo_service.exe` يعمل
- تحقق من pipe name في كلا الطرفين
- جرّب pipe name مختلف باستخدام `--pipe` flag

### المشكلة: Extension تعرض "Disconnected"
1. تأكد أن `remo_service.exe` يعمل
2. تحقق من Registry:
   ```powershell
   Get-ItemProperty "HKCU:\Software\Google\Chrome\NativeMessagingHosts\com.remoodownload.native_host"
   ```
3. تحقق أن المسار في manifest يشير لـ `bin\remo_native_host.exe`:
   ```powershell
   Get-Content "C:\Users\remon\AppData\Local\RemooDownload\native-messaging\com.remoodownload.native_host.json"
   ```
4. أعد تشغيل Chrome **تمامًا**

### المشكلة: التحميلات لا تُعترض
- تأكد أن "Intercept downloads" مفعّل في popup
- تحقق من حجم الملف (الافتراضي: ≥ 10 MB)
- تحقق من امتداد الملف (يجب أن يكون في القائمة)
- افتح Console في Chrome (F12) وابحث عن أخطاء JavaScript

### المشكلة: الملف لا يظهر على القرص
- تأكد أن المسار `D:\TestDownloads` موجود
- تحقق من صلاحيات الكتابة
- تحقق من المساحة المتاحة
- راجع logs في terminal الخدمة
- تأكد أن الـ URL صالح (جرّب curl أو wget)

---

## 📚 المراجع الكاملة

### للتحقق التفصيلي:
📖 **`docs/V0.5-MANUAL-VERIFICATION.md`** - دليل 8 أجزاء كامل

### للمطورين:
📖 **`BUILD_AND_TEST_V05.md`** - دليل بناء واختبار سريع
📖 **`docs/V0.5-RELEASE-NOTES.md`** - ملاحظات إصدار شاملة (English)
📖 **`docs/V0.5-SUMMARY-AR.md`** - ملخص كامل بالعربية

### للتوثيق:
📖 **`tools/README.md`** - دليل الأدوات
📖 **`extensions/chromium/PRIVACY_POLICY.md`** - سياسة الخصوصية

---

## ⏰ الوقت المتوقع

- **الاختبار الأساسي (CLI):** 5 دقائق
- **تسجيل Native Host:** 2 دقيقة
- **تحميل Extension:** 3 دقائق
- **اختبار End-to-End:** 5 دقائق

**الإجمالي: ~15-20 دقيقة**

---

## 🎯 هدف واضح

**الهدف:** تأكد بدليل قاطع (ملف حقيقي اتنزل على القرص + تحديث حي في الواجهة الرسومية) إن السلسلة كاملة شغالة:

```
Browser Click → Extension → Native Host → IPC → Service → DownloadEngine → Real File ✓
```

---

## 🚀 ابدأ الآن!

**كل شيء جاهز. افتح Terminal الأول وابدأ!**

```powershell
cd d:\remoo-download\build\gui-debug\bin
.\remo_service.exe
```

**حظًا موفقًا! 🎯🚀**
